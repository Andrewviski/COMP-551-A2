Run the following commands to generated preprocessed data:

```
cd preprocess
bash preprocess.bash
python2 vectorize.py
```

Note that you have to cd into the 'preprocess' folder before running the bash script